name: covalent_probe_analysis
channels:
  - conda-forge
dependencies:
  - python=3.12
  - numpy
  - matplotlib
  - tqdm
  - jupyter
  - jupyterlab
  - ipykernel
  - pip
  - pip:
      - -r requirements.txt  # Install other dependencies through pip